<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://kit.fontawesome.com/29653ac336.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/productEdit.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500&family=Raleway:wght@300&display=swap"
        rel="stylesheet">
    <title>Document</title>
</head>

<body>
    <%- include('./partials/header.ejs'); %>
        <main>
            <form action="/tienda/edit/<%= productToEdit.id %>?_method=PUT" method="POST" class="edit-form" enctype="multipart/form-data">
                <h2>Editando: <%= productToEdit.name %>
                </h2>
                <div class="container-data-product">
                    <label for="name" class="form-label">Nombre del producto:</label>
                    <input type="text" id="name" name="name" value="<%= productToEdit.name %>" class="form-input">

                    <label for="price" class="form-label">Precio del producto:</label>
                    <input type="text" id="price" name="price" value="<%= productToEdit.price %>" class="form-input">

                    <label for="preview" class="form-label">Preview:</label>
                    <input type="text" id="preview" name="preview" value="<%= productToEdit.preview %>"
                        class="form-input">

                    <label for="category" class="form-label">Categoría:</label>

                    <div class="content-select">
                        <select name="category" id="category" class="form-input">
                            <option value="">Elegí</option>
                            <option value="in-sale">En oferta</option>
                            <option value="visited">Últimos agregados</option>
                        </select>
                    </div>
                    <label for="description" class="form-label" >Descipción:</label>
                    <textarea name="description" id="description" placeholder="Descripcion completa del producto" class="form-input"><%= productToEdit.description %></textarea>

                    <label for="product-image" class="form-label">Imagen:</label>
                    <input type="file" name="product-image" id="product-image" class="custom-file-input">

                    <button type="submit" class="edit-button">Editar producto <%= productToEdit.id %></button>
                </div>
            </form>
        </main>
        <%- include('./partials/footer.ejs'); %>
</body>

</html>